import 'package:flutter/material.dart';

class Pub extends StatelessWidget {
  const Pub({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Pub Administration")),
      body: ListView(
        children: [],
      ),
    );
  }
}
